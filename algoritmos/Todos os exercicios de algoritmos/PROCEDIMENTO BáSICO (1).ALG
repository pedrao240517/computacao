algoritmo "procedimento básico"
var
 X, Y:inteiro
procedimento soma (A, B: inteiro)
inicio
    Escreval("Recebi o valor ", A)
    Escreval("Recebi o valor ", B)
    Escreval(" A soma entre os dois é ", A + B)
fimprocedimento
inicio
     X <- 5
     Y <- 3
     Soma(X ,Y)

fimalgoritmo